function replaceImage() {  
    var imageElement = document.getElementById('image');  
    imageElement.src = 'j.jpg'; // Replace with the new image source  
    imageElement.alt = 'New Image'; // Update alt text if needed  
}